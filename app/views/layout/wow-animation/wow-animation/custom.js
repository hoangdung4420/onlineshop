var wow = new WOW(
  {
    boxClass:     'wow',      // animated element sass class (default is wow)
    animateClass: 'animated', // animation sass class (default is animated)
    offset:       0,          // distance to the element when triggering the animation (default is 0)
    mobile:       true        // trigger animations on mobile devices (true is default)
  }
);
wow.init();